# Copy to .env (which is gitignored) and fill in.  NEVER commit .env.
# Use a freshly rotated key — the one shared in chat should be revoked.
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides:
# ANTHROPIC_MODEL=claude-haiku-4-5-20251001
# MODAL_BASE=https://alexlee--cell-world-model-demo-web.modal.run
# PROXY_PORT=8787
